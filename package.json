{
  "scripts": {
    "start": "node index.js",
    "build": "echo 'No build step required'",
    "test": "echo 'No test step required'",
    "deploy": "gh-pages -d public"  // Exemplo usando gh-pages para deploy
  }
}
