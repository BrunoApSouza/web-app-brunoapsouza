const path = require('path');

module.exports = {
  entry: './index.js',  // ajuste o caminho conforme necessário
  output: {
    path: path.resolve(__dirname, 'dist'),  // ajuste o diretório de saída conforme necessário
    filename: 'bundle.js'
  },
  mode: 'production'
  // adicione outros plugins, loaders e configurações conforme necessário
};
