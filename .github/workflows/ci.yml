name: CI/CD Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '14'

      - name: Install dependencies
        run: npm install

      # Exemplo de execução de testes, ajuste conforme necessário
      - name: Run tests
        run: npm test

  deploy:
    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '14'

      - name: Install dependencies
        run: npm install

      # Aqui você deve ajustar para o seu script de build
      - name: Build
        run: npm run build

      # Deploy para GitHub Pages
      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.PERSONAL_ACCESS_TOKEN }}  # Ajuste aqui para o nome correto do seu segredo
<<<<<<< HEAD
          publish_dir: ./  # Ajuste conforme necessário, se o seu build estiver em um diretório diferente
=======
          publish_dir: ./  # Ajuste conforme necessário, se o seu build estiver em um diretório diferente
>>>>>>> 79f9c6c98d7aeec0e24ed63a8880b890fbadcfb0
